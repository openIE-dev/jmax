# hello

The minimum JMax program: ranges, element-wise operators, printing.

```bash
jmax run hello.jm
```

What this shows:
- Range syntax `1..10`
- Element-wise broadcast operator `.^`
- `println` with mixed args
